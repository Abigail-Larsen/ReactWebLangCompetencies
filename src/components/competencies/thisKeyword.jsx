import React from 'react';
import {
    Link
} from "react-router-dom";
import styled from '@emotion/styled';
import {
    Typography
} from '@material-ui/core';
import { CompList } from '../../constants/index'

export const ThisKeyword = () => {
    // console.log("hey'", CompList)
    return (
        <div>
            this keyword COMP
        </div>
    )
}
